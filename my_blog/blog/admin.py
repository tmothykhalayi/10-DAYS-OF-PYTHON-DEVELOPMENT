from django.contrib import admin
from .models import Post  # Import the Post model

# Register the Post model so it appears in the admin panel
admin.site.register(Post)

# Register your Post model here
admin.site.register(Post)
